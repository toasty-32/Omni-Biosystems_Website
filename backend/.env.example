FIREBASE_PROJECT_ID=omni-biosystems

# dev | production
ENVIRONMENT=development

# Comma-separated CORS origins
# Dev:        http://localhost:3000
# Production: https://omni-biosystems.com,https://www.omni-biosystems.com
ALLOWED_ORIGINS=http://localhost:3000

# Path to a service account key JSON for local dev (not needed on Cloud Run)
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json
